# Configuración del servidor
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Base de datos PostgreSQL (desarrollo local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=MedicalInventary
DB_USER=postgres
DB_PASSWORD=tu_contraseña

# Connection string completa — Railway / producción (reemplaza las vars individuales)
# DATABASE_URL=postgresql://usuario:contraseña@host:port/dbname

# JWT
JWT_SECRET=cambiar_este_secreto_en_produccion
JWT_EXPIRES_IN=8h
